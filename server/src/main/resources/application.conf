ktor {
    deployment {
        port = 8080
    }
    application {
        modules = [ org.rudolf.ApplicationKt.module ]
    }
    git {
        repositoryUrl = "https://github.com/rudolf1/bigDecimalPlugin.git"
        branchRefreshIntervalMinutes = 5
    }
}
